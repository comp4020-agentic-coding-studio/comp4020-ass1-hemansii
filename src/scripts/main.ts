import { initAngleChart } from "./chart";
import { initAeroExplainer } from "./interaction";
import { initCarVisual } from "./car";
import { initPresets } from "./presets";

initAeroExplainer(document);
initAngleChart(document);
initCarVisual(document);
initPresets(document);
